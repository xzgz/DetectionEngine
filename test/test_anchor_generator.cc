#include <vector>
#include <iostream>

#include "core/anchor/anchor_generator.h"

using namespace std;
using namespace cobjectflow;

int main() {
//  float32_t base_size = 8.0;
//  vector<float> scales = {8.0};
//  vector<float> ratios = {0.5, 1.0, 2.0};
//  vector<float> anchor_center(0);
////  vector<float> anchor_center = {0};
//  bool_t scale_major = false;
//  AnchorGenerator<float> anchor_generator(base_size, scales, ratios, anchor_center,
//                                          scale_major);
//  anchor_generator.GenBaseAnchors();
//
//  vector<int32_t> featmap_size = {20, 20};
//  int32_t stride = 8;
//  anchor_generator.GenGridAnchors(featmap_size, stride=8);
//
//  cout << "base_anchor_count: " << anchor_generator.base_anchor_count() << endl;
//  cout << scales.size() * ratios.size() << endl;
//  cout << "grid_anchor_count: " << anchor_generator.grid_anchor_count() << endl;
//  cout << featmap_size[0] * featmap_size[1] * scales.size() * ratios.size() << endl;
//
//  cout << "base anchors:\n";
//  anchor_generator.PrintBaseAnchors();
//  boost::shared_ptr<Anchor<float> > base_anchors = anchor_generator.base_anchors();
//  cout << "base anchor_heads size:\n";
//  anchor_generator.PrintBaseAnchorSize();
//  cout << "base anchors shape:\n";
//  cout << base_anchors->anchor_shape[0] << " " << base_anchors->anchor_shape[1] << endl;
//
//  cout << "grid anchors:\n";
//  anchor_generator.PrintGridAnchors();
//  boost::shared_ptr<Anchor<float> > grid_anchors = anchor_generator.grid_anchors();
//  cout << "grid anchors size:\n";
//  anchor_generator.PrintGridAnchorSize();
//  cout << "grid anchor_heads shape:\n";
//  cout << grid_anchors->anchor_shape[0] << " " << grid_anchors->anchor_shape[1] << endl;
}



